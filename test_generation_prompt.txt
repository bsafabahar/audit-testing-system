شما باید یک فایل پایتون برای آزمون حسابرسی بسازید که دقیقا از ساختار زیر پیروی کند:

***

## بخش 1: METADATA و NAMING

### نامگذاری فایل:
نام فایل: `{{test_name}}.py` (با snake_case)

**الزامات CRITICAL برای نام فایل:**
1. نام فایل باید **DESCRIPTIVE** و **MEANINGFUL** باشد
2. نام فایل باید موضوع آزمون را کاملاً توصیف کند
3. نام فایل باید به صورت snake_case باشد (حروف کوچک + underscore)
4. **هرگز** از نام‌های عمومی مثل "custom_test" یا "my_test" استفاده نکن
5. نام باید شامل موضوع اصلی آزمون باشد

**مثال‌های صحیح:**
- `inventory_negative_balance_test.py` ❌ خیر! بیش از حد طولانی
- `negative_inventory_test.py` ✅ بله! خلاصه و گویا
- `duplicate_invoice_detection_test.py` ❌ خیر! بیش از حد طولانی  
- `duplicate_invoices_test.py` ✅ بله! خلاصه و گویا
- `benford_second_digit_test.py` ✅ بله! کامل و دقیق
- `revenue_trend_analysis_test.py` ❌ خیر! بیش از حد طولانی
- `revenue_trend_test.py` ✅ بله!

**مثال‌های غلط - هرگز استفاده نکن:**
- `custom_test.py` ❌ عمومی و بی‌معنی
- `my_test.py` ❌ عمومی و بی‌معنی  
- `test1.py` ❌ بی‌معنی
- `audit_test.py` ❌ خیلی عمومی

***

## بخش 2: DOCSTRING هدر
ابتدای فایل یک docstring سهخطی با این ساختار:

```python
"""
{{عنوان فارسی آزمون - کوتاه و گویا}}
{{English Test Title - Short and Descriptive}}

{{توضیح فارسی آزمون - حداقل یک جمله کامل}}
{{توضیح تکمیلی - اختیاری}}
"""
```

**الزامات CRITICAL:**
- دقیقا با سه علامت نقل قول شروع و پایان
- خط اول: عنوان فارسی (کوتاه، حداکثر 50 کاراکتر)
- خط دوم: عنوان انگلیسی (کوتاه، حداکثر 50 کاراکتر، از این خط نام فایل استخراج می‌شود)
- خط سوم: خالی
- خطوط بعدی: توضیح فارسی کامل

**نکات مهم برای خط دوم (English Title):**
- باید DESCRIPTIVE باشد (نه "Custom Test" یا "My Test")
- باید موضوع آزمون را کاملاً بیان کند
- از این عنوان نام فایل ساخته می‌شود
- مثال صحیح: "Negative Inventory Balance Detection"
- مثال غلط: "Custom Test" یا "Test"

***

## بخش 3: IMPORTS

### 3.1 Imports استاندارد (الزامی - دقیقا به این ترتیب):
```python
from typing import List, Dict, Any
from models import Transaction
from parameters import param_string, param_number
from schema import col, schema
from query_runner import get_parameter
from types_definitions import QueryDefinition
from database import ReadOnlySession
```

**نکات:**
- `param_string` و `param_number` را فقط آنهایی import کن که استفاده میشوند
- اگر آزمون param_boolean یا param_date نیاز دارد، آنها را هم اضافه کن

### 3.2 Imports کتابخانههای پایتون (بر اساس نیاز):
مثالها:
```python
from collections import Counter
from datetime import datetime
import math
import warnings
```

### 3.3 Imports کتابخانههای خارجی با مدیریت خطا (اگر نیاز است):
**الگوی استاندارد:**
```python
import warnings
warnings.filterwarnings('ignore')

try:
    from {{library_name}} import {{modules}}
    {{LIBRARY}}_AVAILABLE = True
except ImportError:
    {{LIBRARY}}_AVAILABLE = False
```

**مثال واقعی:**
```python
try:
    from sklearn.ensemble import IsolationForest
    from sklearn.preprocessing import StandardScaler
    import numpy as np
    SKLEARN_AVAILABLE = True
except ImportError:
    SKLEARN_AVAILABLE = False
```

***

## بخش 4: CONSTANTS (اختیاری)
اگر آزمون به ثابتهایی مثل آرایه، دیکشنری یا مقادیر از پیش تعریف شده نیاز دارد:

```python
# {{کامنت فارسی توضیح ثابت}}
CONSTANT_NAME = {{
    # مقادیر
}}
```

**مثال:**
```python
# توزیع بنفورد مورد انتظار
BENFORD_EXPECTED = {{
    1: 0.301,
    2: 0.176,
    ...
}}
```

***

## بخش 5: تابع define() (الزامی)

```python
def define() -> QueryDefinition:
    """تعریف پارامترها و اسکیما"""
    
    parameters = [
        param_string('{{param1_name}}', '{{توضیح فارسی پارامتر}}', default_value='{{default}}'),
        param_number('{{param2_name}}', '{{توضیح فارسی پارامتر}}', default_value={{number}}),
        # سایر پارامترها
    ]
    
    result_schema = schema(
        col('{{Column1Name}}', '{{توضیح فارسی ستون}}', '{{data_type}}'),
        col('{{Column2Name}}', '{{توضیح فارسی ستون}}', '{{data_type}}'),
        # سایر ستونها
    )
    
    return {{
        'parameters': parameters,
        'schema': result_schema
    }}
```

### قوانین parameters:
- هر پارامتر روی یک خط جداگانه
- نام پارامترها: camelCase (حرف اول کوچک)
- انواع param: `param_string`, `param_number`, `param_boolean`, `param_date`
- default_value الزامی است
- توضیحات به فارسی

### قوانین schema (result_schema):
- نام ستونها: PascalCase (حرف اول بزرگ)
- توضیحات ستونها: فارسی
- انواع داده مجاز:
  - `'string'`: متن
  - `'integer'`: عدد صحیح
  - `'number'`: عدد اعشاری
  - `'currency'`: مبلغ پولی
  - `'percent'`: درصد
  - `'date'`: تاریخ
  - `'boolean'`: بله/خیر

### ستونهای رایج در Transaction:
- TransactionID یا Id
- DocumentDate
- AccountCode
- Debit (بدهکار)
- Credit (بستانکار)
- Description
- TransactionDate

***

## بخش 6: توابع کمکی (اختیاری)

```python
def helper_function_name(param1: type, param2: type) -> return_type:
    """توضیح فارسی تابع"""
    # کامنت فارسی
    # منطق تابع
    return result
```

**الزامات:**
- Type hints برای پارامترها و return
- Docstring فارسی
- کامنتهای فارسی برای بخشهای کلیدی

***

## بخش 7: تابع execute() (الزامی)

```python
def execute(session: ReadOnlySession) -> List[Dict[str, Any]]:
    """اجرای آزمون {{نام آزمون}}"""
```

### ساختار استاندارد execute:

#### 7.1 بررسی وابستگیهای خارجی (اگر دارد):
```python
    if not {{LIBRARY}}_AVAILABLE:
        return [{{
            '{{FirstColumn}}': 'ERROR',
            # سایر ستونها با مقادیر پیشفرض
            '{{MessageColumn}}': 'Library not installed',
        }}]
```

#### 7.2 دریافت پارامترها:
```python
    # دریافت پارامترها
    param1 = get_parameter('{{param1Name}}', '{{default_value}}')
    param2 = get_parameter('{{param2Name}}', {{default_number}})
```

**نکات:**
- کامنت فارسی "# دریافت پارامترها"
- نام متغیرها: snake_case
- مقادیر پیشفرض باید با define() یکسان باشند

#### 7.3 دریافت دادهها از دیتابیس:
```python
    # دریافت دادهها
    query = session.query(Transaction)
    results = query.all()
```

#### 7.4 پردازش دادهها:
با کامنتهای فارسی مناسب:
```python
    # {{شرح مرحله پردازش}}
    data_list = []
    
    for t in results:
        # منطق پردازش
        # بررسی شرایط
        # محاسبات
```

**الگوهای رایج:**
- استفاده از Counter برای شمارش
- حلقههای for روی results
- شرطهای if برای فیلتر کردن
- محاسبات آماری

#### 7.5 بررسی حداقل داده:
اگر آزمون به حداقل تعداد رکورد نیاز دارد:
```python
    if len(data_list) < {{minimum_required}}:
        return [{{
            '{{Column1}}': 'ERROR',
            '{{MessageColumn}}': 'Not enough transactions (minimum {{number}})',
            # سایر ستونها
        }}]
```

#### 7.6 ساخت خروجی:
```python
    # {{توضیح فارسی}}
    data = []
    
    for item in items:
        row = {{
            '{{Column1Name}}': value1,
            '{{Column2Name}}': value2,
            '{{Column3Name}}': round(value3, {{decimals}}),
            # سایر ستونها
        }}
        data.append(row)
```

**قوانین row dictionary:**
- کلیدها: دقیقا همان نامهای ستونها در schema
- مقادیر عددی: از round() برای اعشار استفاده کن
- تاریخ: به صورت string با format مناسب
- null values: از None یا '' استفاده کن

#### 7.7 مرتبسازی (اختیاری):
```python
    # مرتبسازی بر اساس {{معیار}}
    data.sort(key=lambda x: x['{{ColumnName}}'], reverse={{True/False}})
```

#### 7.8 اضافه کردن ردیف خلاصه (اختیاری):
اگر آزمون نیاز به ردیف جمع یا خلاصه دارد:
```python
    # افزودن ردیف خلاصه
    data.append({{
        '{{Column1}}': {{summary_value}},
        '{{Column2}}': {{total_value}},
        # سایر ستونها
    }})
```

#### 7.9 بازگشت نتیجه:
```python
    return data
```

***

## بخش 8: قوانین FORMATTING

### فاصلهگذاری:
- دو خط خالی بین توابع سطح بالا
- یک خط خالی بین بخشهای منطقی داخل توابع
- بدون فضای خالی اضافی در انتهای خطوط

### کامنتها:
- همه کامنتها به فارسی
- کامنتهای بخش با # در ابتدای خط
- کامنتهای inline در صورت نیاز

### نامگذاری:
- توابع: snake_case
- متغیرها: snake_case  
- ثابتها: UPPER_SNAKE_CASE
- پارامترها: camelCase
- ستونهای schema: PascalCase

### Type hints:
- همیشه برای پارامترهای توابع
- همیشه برای return type
- برای متغیرهای مهم

***

## بخش 9: الگوهای رایج در execute()

### 9.1 کار با ستونهای Debit/Credit:
```python
column_name = get_parameter('columnName', 'Debit')
amount = t.Debit if column_name == 'Debit' else t.Credit
if not amount or amount <= 0:
    continue
```

### 9.2 کار با تاریخ:
```python
if hasattr(t, 'TransactionDate') and t.TransactionDate:
    trans_date = t.TransactionDate
elif hasattr(t, 'DocumentDate') and t.DocumentDate:
    if isinstance(t.DocumentDate, datetime):
        trans_date = t.DocumentDate
    else:
        trans_date = datetime.combine(t.DocumentDate, datetime.min.time())
```

### 9.3 مدیریت TransactionID:
```python
trans_id = str(t.TransactionID) if hasattr(t, 'TransactionID') and t.TransactionID else str(t.Id)
```

### 9.4 Format کردن تاریخ برای خروجی:
```python
'DocumentDate': trans_date.strftime('%Y-%m-%d') if trans_date else ''
```

### 9.5 مدیریت None values:
```python
'AccountCode': t.AccountCode or ''
'Description': t.Description if t.Description else 'N/A'
```

***

## بخش 10: فایل راهنمای md (الزامی)

علاوه بر فایل پایتون، باید یک فایل راهنمای markdown هم تولید شود. این راهنما باید در کنار فایل پایتون قرار گیرد و ساختار زیر را داشته باشد:

### نامگذاری فایل راهنما:
- نام فایل: `{{test_name}}.md` (همان نام فایل پایتون اما با پسوند md)
- مثال: اگر فایل پایتون `negative_inventory_test.py` است، فایل راهنما `negative_inventory_test.md` خواهد بود

### ساختار فایل راهنما (md):

```markdown
# {{عنوان کامل فارسی آزمون}}

## 🎯 هدف آزمون

{{توضیح کامل هدف آزمون - حداقل 2-3 جمله}}
{{چه مشکلی را حل می‌کند؟ چه چیزی را بررسی می‌کند؟}}

## 💡 چرا از این آزمون در حسابرسی استفاده می‌شود؟

### کاربردهای حسابرسی:
1. **{{کاربرد اول}}**: {{توضیح}}
2. **{{کاربرد دوم}}**: {{توضیح}}
3. **{{کاربرد سوم}}**: {{توضیح}}
4. **{{کاربرد چهارم}}**: {{توضیح}} (اختیاری)
5. **{{کاربرد پنجم}}**: {{توضیح}} (اختیاری)

### مثال‌های کاربردی:
- {{مثال کاربردی اول}}
- {{مثال کاربردی دوم}}
- {{مثال کاربردی سوم}}
- {{مثال کاربردی چهارم}} (اختیاری)

## 📥 پارامترهای ورودی

{{برای هر پارامتری که در define() تعریف شده، یک بخش ایجاد کن:}}

### 1. {{parameterName}} ({{نام فارسی پارامتر}})
- **نوع**: {{نوع داده - مثلاً رشته (String)، عدد (Number)، تاریخ (Date)}}
- **الزامی**: {{بله یا خیر}}
- **توضیحات**: {{توضیح کامل پارامتر}}
- **مقدار پیش‌فرض**: {{مقدار پیش‌فرض}}
- **مقادیر مجاز** (در صورت وجود محدودیت):
  - {{مقدار 1}}: {{توضیح}}
  - {{مقدار 2}}: {{توضیح}}
- **راهنما** (اختیاری):
  - {{راهنمای استفاده}}
- **نکته** (اختیاری): {{نکات مهم}}

### 2. {{parameterName2}} ({{نام فارسی پارامتر}})
- ...

## 📤 پارامترهای خروجی

### ستون‌های خروجی:

{{برای هر ستونی که در schema تعریف شده، یک بخش ایجاد کن:}}

1. **{{ColumnName}} ({{نام فارسی ستون}})**
   - {{توضیح کامل ستون}}
   - {{نحوه محاسبه یا منبع داده}}
   - فرمول (اگر دارد): {{فرمول}}

2. **{{Column2Name}} ({{نام فارسی ستون}})**
   - {{توضیح}}
   
3. **{{Column3Name}} ({{نام فارسی ستون}})**
   - {{توضیح}}

## 🔄 نحوه انجام آزمون

### مراحل اجرا:

#### مرحله 1: {{عنوان مرحله اول}}
{{توضیح کامل مرحله اول}}
{{در صورت نیاز، کد یا فرمول به صورت code block}}

```
{{pseudocode یا توضیح الگوریتم}}
```

#### مرحله 2: {{عنوان مرحله دوم}}
{{توضیح کامل مرحله دوم}}

**مثال:**
```
{{مثال واقعی}}
```

#### مرحله 3: {{عنوان مرحله سوم}}
{{توضیح}}

### محاسبات:

**{{نام محاسبه 1}}:**
```
فرمول = {{فرمول}}
```

**{{نام محاسبه 2}}:**
```
فرمول = {{فرمول}}
```

## 📊 تفسیر نتایج

### معیارهای ارزیابی:

#### {{معیار اول}}:
- **{{وضعیت خوب}}**: {{توضیح}}
- **{{وضعیت متوسط}}**: {{توضیح}}
- **{{وضعیت بد}}**: {{توضیح}}

#### {{معیار دوم}}:
- ...

### نشانه‌های هشدار (Red Flags):

⚠️ **{{نشانه هشدار 1}}**: {{توضیح}}

⚠️ **{{نشانه هشدار 2}}**: {{توضیح}}

⚠️ **{{نشانه هشدار 3}}**: {{توضیح}}

## 🎓 مثال کاربردی

### سناریو:
{{شرح یک سناریوی واقعی}}

### داده‌های ورودی:
{{نمونه داده‌های ورودی}}

### نتیجه مورد انتظار:
{{نتیجه‌ای که از آزمون انتظار می‌رود}}

### تفسیر:
{{تفسیر نتیجه}}

## ⚙️ پیکربندی پیشنهادی

### برای {{نوع کسب‌وکار یا صنعت}}:
- **{{پارامتر}}**: {{مقدار پیشنهادی}}
- **{{پارامتر}}**: {{مقدار پیشنهادی}}

### برای {{نوع دیگر}}:
- **{{پارامتر}}**: {{مقدار پیشنهادی}}

## 📝 نکات مهم

1. **{{نکته مهم اول}}**: {{توضیح}}
2. **{{نکته مهم دوم}}**: {{توضیح}}
3. **{{نکته مهم سوم}}**: {{توضیح}}

## ⚠️ محدودیت‌ها

1. {{محدودیت اول}}
2. {{محدودیت دوم}}
3. {{محدودیت سوم}} (در صورت وجود)

## 🔗 آزمون‌های مرتبط

- **{{نام آزمون مرتبط 1}}**: {{چرا مرتبط است}}
- **{{نام آزمون مرتبط 2}}**: {{چرا مرتبط است}}
- **{{نام آزمون مرتبط 3}}**: {{چرا مرتبط است}} (اختیاری)

## 📚 منابع و استانداردها

### استانداردهای حسابرسی:
- {{استاندارد 1}}
- {{استاندارد 2}}

### منابع علمی:
- {{منبع 1}}
- {{منبع 2}} (اختیاری)
```

### نکات مهم برای تولید فایل راهنما:

1. **زبان**: تمام محتوا باید به فارسی باشد (به جز اصطلاحات تخصصی که معادل فارسی ندارند)
2. **ایموجی‌ها**: از ایموجی‌های مناسب برای هر بخش استفاده کن (🎯 💡 📥 📤 🔄 📊 🎓 ⚙️ 📝 ⚠️ 🔗 📚)
3. **جزئیات**: توضیحات باید کامل و قابل فهم باشند
4. **مثال‌ها**: حتماً مثال‌های واقعی و کاربردی بیاور
5. **فرمول‌ها**: اگر محاسباتی وجود دارد، فرمول‌ها را واضح بنویس
6. **سازگاری**: محتوای فایل md باید کاملاً با کد پایتون سازگار باشد

### خروجی نهایی:

شما باید **دو فایل** تولید کنید:

1. **فایل پایتون** (`{{test_name}}.py`): کد کامل آزمون
2. **فایل راهنما** (`{{test_name}}.md`): مستندات کامل فارسی

**فرمت خروجی:**

ابتدا کد پایتون را بنویس:
```python
{{کد کامل پایتون}}
```

سپس بلافاصله بعد از آن، فایل راهنما را با علامت جداکننده مشخص بنویس:

---MARKDOWN_FILE---
```markdown
{{محتوای کامل فایل md}}
```

***

## بخش 11: TEMPLATE نهایی استفاده

حالا با استفاده از تمام بخشهای بالا، آزمون زیر را تولید کن:

**[در اینجا تعریف آزمون کاربر قرار میگیرد]**

{user_description}

***

## دستورالعملهای اجرایی - CRITICAL:

### ✅ الزامات نامگذاری (بسیار مهم!):
1. **عنوان انگلیسی (خط دوم docstring):**
   - باید دقیقاً موضوع آزمون را توصیف کند
   - نه "Custom Test"، نه "My Test"، نه "Test"
   - مثال: "Negative Inventory Balance Detection"
   - مثال: "Duplicate Invoice Detection"
   - مثال: "Benford Second Digit Analysis"

2. **نام فایل:**
   - از عنوان انگلیسی استخراج می‌شود
   - باید snake_case باشد
   - باید کوتاه و گویا باشد (حداکثر 40 کاراکتر)
   - **هرگز** custom_test.py نباشد

### ✅ سایر الزامات:
3. Docstring را با توضیحات کامل فارسی و انگلیسی بنویس
4. Imports لازم را شناسایی و به ترتیب صحیح import کن
5. اگر کتابخانه خارجی نیاز است، try-except بگذار
6. اگر ثابتهایی نیاز است، تعریف کن
7. تابع define() را با پارامترها و schema دقیق بنویس
8. توابع کمکی مورد نیاز را بنویس
9. تابع execute() را با تمام مراحل استاندارد بنویس
10. همه کامنتها و docstring ها فارسی باشند
11. خروجی باید List[Dict[str, Any]] باشد
12. کلیدهای dict باید دقیقا همان نام ستونهای schema باشند
13. از round() برای اعداد اعشاری استفاده کن
14. **حتماً فایل راهنمای md را هم تولید کن**

### ⚠️ قوانین EXECUTE:
- همیشه بررسی کن که داده کافی وجود دارد
- از try-except برای مدیریت خطا استفاده کن
- مقادیر None را به درستی handle کن
- از hasattr() برای بررسی فیلدها استفاده کن
- خروجی همیشه باید list باشد (حتی در صورت خطا)

### ⚠️ قوانین فایل راهنما (md):
- فایل md باید کامل و جامع باشد
- تمام پارامترها و ستون‌ها توضیح داده شوند
- مثال‌های واقعی بیاور
- از ایموجی‌های مناسب استفاده کن
- محتوا کاملاً به فارسی باشد

### 📋 فرمت خروجی نهایی:

ابتدا کد پایتون را بنویس، سپس بعد از علامت `---MARKDOWN_FILE---` فایل md را بنویس:

```python
{{کد کامل پایتون}}
```

---MARKDOWN_FILE---

```markdown
{{محتوای کامل فایل راهنما}}
```

فقط کد پایتون و فایل راهنما را بدون توضیحات اضافی برگردان. هر دو فایل باید آماده استفاده باشند.
