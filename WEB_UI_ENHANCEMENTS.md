# سیستم آزمون‌های حسابرسی - رابط کاربری وب پیشرفته

## 🎯 خلاصه تغییرات

این پروژه شامل یک رابط کاربری وب پیشرفته با پشتیبانی کامل RTL (راست‌چین) برای اجرای 53 آزمون حسابرسی می‌باشد.

## ✨ ویژگی‌های جدید

### 1. رابط کاربری مدرن و زیبا
- ✅ طراحی کاملاً راست‌چین (RTL) با فونت‌های فارسی
- ✅ گرادیان‌های رنگی زیبا و انیمیشن‌های روان
- ✅ طراحی واکنش‌گرا (Responsive) برای موبایل، تبلت و دسکتاپ
- ✅ کارت‌های دسته‌بندی شده برای 53 آزمون حسابرسی
- ✅ آیکون‌های مناسب برای هر آزمون

### 2. نمایش توضیحات آزمون‌ها
- ✅ دکمه اطلاعات (ℹ️) برای هر آزمون
- ✅ Modal زیبا برای نمایش توضیحات کامل از فایل‌های .md
- ✅ فرمت‌بندی Markdown به HTML
- ✅ نمایش پارامترهای ورودی، خروجی و مثال‌های کاربردی

### 3. قابلیت جستجو و فیلتر
- ✅ جعبه جستجوی لحظه‌ای برای یافتن سریع آزمون‌ها
- ✅ فیلتر کردن دسته‌بندی‌ها بر اساس نام آزمون
- ✅ هایلایت نتایج جستجو

### 4. آپلود فایل اکسل
- ✅ بخش آپلود فایل با طراحی Drag & Drop
- ✅ پردازش خودکار فایل اکسل
- ✅ وارد کردن داده‌ها به دیتابیس
- ✅ نمایش پیشرفت و وضعیت

### 5. اجرای آزمون‌ها
- ✅ اجرای تک‌تک آزمون‌ها با یک کلیک
- ✅ اجرای همه آزمون‌ها به صورت دسته‌جمعی
- ✅ نمایش نتایج در جدول‌های زیبا
- ✅ خروجی اکسل از نتایج

### 6. آمار و گزارش
- ✅ کارت‌های آماری برای نمایش تعداد رکوردها
- ✅ شمارنده آزمون‌های اجرا شده
- ✅ نمایش تعداد کل آزمون‌های موجود

## 🧪 تست‌های Playwright

### آزمون‌های اجرا شده (17/17 موفق ✅)

1. **test_page_loads** - بارگذاری صفحه اصلی
2. **test_upload_section_visible** - نمایش بخش آپلود
3. **test_all_test_categories_visible** - نمایش همه دسته‌بندی‌ها
4. **test_search_functionality** - عملکرد جستجو
5. **test_test_info_modal** - نمایش Modal اطلاعات
6. **test_file_upload_without_file** - اعتبارسنجی آپلود
7. **test_run_all_tests_confirmation** - تایید اجرای همه آزمون‌ها
8. **test_ui_responsiveness** - واکنش‌گرایی UI
9. **test_test_items_clickable** - قابلیت کلیک آیتم‌ها
10. **test_stats_hidden_initially** - مخفی بودن اولیه آمار
11. **test_results_section_hidden_initially** - مخفی بودن بخش نتایج
12. **test_visual_design_elements** - عناصر طراحی
13. **test_accessibility_rtl** - دسترسی‌پذیری RTL
14. **test_modal_close_functionality** - بستن Modal
15. **test_export_button_disabled_initially** - غیرفعال بودن دکمه خروجی
16. **test_animations_present** - وجود انیمیشن‌ها
17. **test_persian_font_family** - فونت‌های فارسی

### اجرای تست‌ها

```bash
# نصب وابستگی‌ها
pip install -r requirements.txt

# نصب مرورگر Playwright
python -m playwright install chromium

# اجرای سرور Flask
python web_ui.py

# اجرای تست‌ها (در ترمینال دیگر)
pytest test_web_ui.py -v
```

## 📸 تصاویر

### صفحه اصلی
![صفحه اصلی](https://github.com/user-attachments/assets/795baa68-a1f6-4463-95c2-52da9154b5c4)

صفحه اصلی با:
- 53 آزمون حسابرسی در 17 دسته‌بندی
- طراحی راست‌چین کامل
- رنگ‌بندی و آیکون‌های زیبا

### Modal توضیحات آزمون
![Modal توضیحات](https://github.com/user-attachments/assets/16150288-8b92-4056-9ea5-75e4c314bbcb)

نمایش توضیحات کامل آزمون شامل:
- هدف آزمون
- کاربردهای حسابرسی
- پارامترهای ورودی و خروجی
- مثال‌های عملی
- نکات مهم و محدودیت‌ها

### جستجوی آزمون‌ها
![جستجو](https://github.com/user-attachments/assets/bc06beed-03a2-42d8-8f49-18b1aad501e2)

قابلیت جستجوی لحظه‌ای:
- فیلتر کردن آزمون‌ها بر اساس نام
- مخفی کردن دسته‌بندی‌های غیرمرتبط
- نمایش فقط نتایج مرتبط

## 🚀 راه‌اندازی

### پیش‌نیازها
- Python 3.8+
- pip
- مرورگر (برای Playwright)

### مراحل نصب

1. **کلون کردن مخزن**
```bash
git clone https://github.com/bsafabahar/audit-testing-system.git
cd audit-testing-system
```

2. **نصب وابستگی‌ها**
```bash
pip install -r requirements.txt
```

3. **نصب مرورگر Playwright**
```bash
python -m playwright install chromium
```

4. **تنظیم دیتابیس**
```bash
# فایل .env را ویرایش کنید
# برای تست از SQLite استفاده می‌شود:
CONNECTION_STRING=sqlite:///audit_test.db

# برای production از SQL Server استفاده کنید:
# CONNECTION_STRING=mssql+pyodbc://username:password@server/database?driver=ODBC+Driver+17+for+SQL+Server
```

5. **اجرای سرور**
```bash
python web_ui.py
```

6. **باز کردن مرورگر**
```
http://localhost:5000
```

## 📝 استفاده

### 1. آپلود فایل اکسل
1. فایل اکسل خود را انتخاب کنید
2. روی "آپلود و وارد کردن" کلیک کنید
3. منتظر بمانید تا داده‌ها وارد شوند

### 2. اجرای آزمون
1. روی هر آزمون کلیک کنید تا اجرا شود
2. یا "اجرای همه آزمون‌ها" را انتخاب کنید
3. نتایج در جدول نمایش داده می‌شوند

### 3. مشاهده توضیحات
1. روی آیکون ℹ️ کنار هر آزمون کلیک کنید
2. Modal با توضیحات کامل باز می‌شود
3. برای بستن روی "بستن" یا خارج از Modal کلیک کنید

### 4. جستجوی آزمون
1. در کادر جستجو تایپ کنید
2. فقط آزمون‌های مرتبط نمایش داده می‌شوند

### 5. خروجی اکسل
1. بعد از اجرای آزمون، روی "خروجی اکسل" کلیک کنید
2. فایل اکسل با نتایج دانلود می‌شود

## 🛠 ساختار فایل‌ها

```
audit-testing-system/
├── web_ui.py                 # سرور Flask
├── templates/
│   └── index.html           # رابط کاربری وب
├── test_web_ui.py           # تست‌های Playwright
├── queries/                 # 53 فایل آزمون
│   ├── *.py                # فایل‌های Python آزمون‌ها
│   └── *.md                # فایل‌های توضیحات
├── models.py               # مدل‌های دیتابیس
├── database.py             # مدیریت دیتابیس
├── requirements.txt        # وابستگی‌ها
└── pytest.ini             # تنظیمات pytest
```

## 🎨 ویژگی‌های طراحی

### رنگ‌بندی
- **Primary**: گرادیان بنفش (#667eea - #764ba2)
- **Success**: سبز (#28a745)
- **Warning**: زرد (#ffc107)
- **Danger**: قرمز (#dc3545)

### فونت‌ها
- **فارسی**: Tahoma, Arial
- **اندازه پایه**: 16px
- **خط-ارتفاع**: 1.8

### انیمیشن‌ها
- Fade in برای ورود صفحه
- Slide down برای Modal
- Hover effects برای دکمه‌ها و کارت‌ها
- Smooth scrolling

## 🔧 تنظیمات پیشرفته

### تغییر پورت سرور
```python
# در فایل web_ui.py
app.run(debug=True, host='0.0.0.0', port=8080)  # تغییر به پورت دلخواه
```

### تنظیم دیتابیس
```python
# در فایل .env
CONNECTION_STRING=mssql+pyodbc://server/database?driver=ODBC+Driver+17+for+SQL+Server
```

## 📊 آمار پروژه

- **تعداد آزمون‌ها**: 53
- **تعداد دسته‌بندی‌ها**: 17
- **تعداد تست‌های Playwright**: 17
- **نرخ موفقیت تست‌ها**: 100%
- **پشتیبانی RTL**: ✅ کامل
- **Responsive Design**: ✅ کامل

## 🤝 مشارکت

برای مشارکت در پروژه:
1. Fork کنید
2. Branch جدید ایجاد کنید
3. تغییرات را commit کنید
4. Push کنید
5. Pull Request ارسال کنید

## 📄 لایسنس

MIT License

## 👨‍💻 توسعه‌دهندگان

توسعه یافته با ❤️ برای حسابداران و حسابرسان ایران

---

**نسخه**: 2.0.0
**تاریخ**: 2026-01-25
