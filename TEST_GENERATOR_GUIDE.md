# راهنمای آزمون‌ساز - Test Generator Guide

## 📝 معرفی

آزمون‌ساز یک ابزار هوشمند است که با استفاده از هوش مصنوعی (OpenAI GPT یا Anthropic Claude)، به صورت خودکار فایل‌های آزمون حسابرسی پایتون تولید می‌کند.

## ✨ ویژگی‌ها

- 🤖 تولید خودکار کد پایتون با استفاده از AI
- 📋 پیروی کامل از استانداردهای پروژه
- 🔧 پشتیبانی از OpenAI (GPT-4, GPT-3.5) و Anthropic (Claude)
- 💾 ذخیره خودکار در پوشه queries
- ✅ کد آماده اجرا و سازگار با سیستم موجود
- 🌐 رابط کاربری فارسی و ساده

## 🚀 نحوه استفاده

### مرحله 1: دسترسی به آزمون‌ساز

1. وارد صفحه اصلی سیستم شوید
2. روی دکمه **🤖 آزمون‌ساز** در هدر کلیک کنید

### مرحله 2: تکمیل فرم

#### 1. توضیحات آزمون (الزامی)
توضیح کاملی از آزمون مورد نظر خود بنویسید. هرچه توضیحات دقیق‌تر باشد، کد تولیدی بهتر خواهد بود.

**مثال‌های خوب:**

```
یک آزمون برای تشخیص تراکنش‌های مشکوک در ساعات غیر اداری (بعد از ساعت 8 شب و قبل از 6 صبح). 
این آزمون باید:
- تاریخ و ساعت تراکنش را بررسی کند
- تراکنش‌هایی که در ساعات غیر اداری انجام شده‌اند را شناسایی کند
- مبلغ، تاریخ، کد حساب و توضیحات را نمایش دهد
- تراکنش‌ها را بر اساس مبلغ نزولی مرتب کند
```

```
آزمون تشخیص اختلاف قیمت خرید برای یک کالا.
- برای هر کد کالا، قیمت خرید را در تراکنش‌های مختلف مقایسه کند
- اگر اختلاف قیمت بیش از 20٪ باشد، گزارش کند
- میانگین، حداقل و حداکثر قیمت را محاسبه کند
- نمودار توزیع قیمت‌ها را ایجاد کند
```

#### 2. نام فایل (اختیاری)
نام دلخواه برای فایل آزمون را وارد کنید. باید به انگلیسی و با فرمت `snake_case` باشد.

- مثال: `after_hours_transactions_test`
- مثال: `price_variance_detection_test`

اگر خالی بگذارید، نام به صورت خودکار از توضیحات استخراج می‌شود.

#### 3. ارائه‌دهنده هوش مصنوعی (الزامی)
یکی از گزینه‌های زیر را انتخاب کنید:

- **OpenAI**: GPT-4 یا GPT-3.5
- **Anthropic**: Claude (Sonnet, Opus)

#### 4. مدل (اختیاری)
مدل خاصی را مشخص کنید یا خالی بگذارید تا مدل پیش‌فرض استفاده شود.

**مدل‌های OpenAI:**
- `gpt-4` (پیشنهادی - کیفیت بالاتر)
- `gpt-4-turbo-preview`
- `gpt-3.5-turbo` (سریع‌تر و ارزان‌تر)

**مدل‌های Anthropic:**
- `claude-3-sonnet-20240229` (پیشنهادی)
- `claude-3-opus-20240229` (قوی‌تر)
- `claude-3-haiku-20240307` (سریع‌تر)

#### 5. کلید API (الزامی)
کلید API خود را از سرویس مورد نظر وارد کنید.

**دریافت کلید OpenAI:**
1. به [platform.openai.com](https://platform.openai.com/) بروید
2. ثبت نام کنید یا وارد شوید
3. به بخش API Keys بروید
4. کلید جدید ایجاد کنید

**دریافت کلید Anthropic:**
1. به [console.anthropic.com](https://console.anthropic.com/) بروید
2. ثبت نام کنید یا وارد شوید
3. به بخش API Keys بروید
4. کلید جدید ایجاد کنید

> ⚠️ **نکته امنیتی**: کلید API شما به صورت امن ارسال می‌شود و هیچ‌جا ذخیره نمی‌گردد.

### مرحله 3: تولید آزمون

1. روی دکمه **🚀 تولید آزمون** کلیک کنید
2. منتظر بمانید تا AI کد را تولید کند (معمولاً 10-30 ثانیه)
3. در صورت موفقیت، کد تولید شده نمایش داده می‌شود
4. فایل به صورت خودکار در پوشه `queries` ذخیره می‌شود

### مرحله 4: استفاده از آزمون

آزمون تولید شده به صورت خودکار در سیستم قابل استفاده است:

1. به صفحه اصلی برگردید
2. آزمون جدید در لیست آزمون‌ها ظاهر می‌شود
3. می‌توانید آن را مانند سایر آزمون‌ها اجرا کنید

## 📊 ساختار آزمون تولیدی

هر آزمون تولید شده شامل موارد زیر است:

### 1. Docstring فارسی-انگلیسی
```python
"""
عنوان فارسی آزمون
English Test Title

توضیحات کامل آزمون به فارسی
"""
```

### 2. Imports استاندارد
```python
from typing import List, Dict, Any
from models import Transaction
from parameters import param_string, param_number
from schema import col, schema
from query_runner import get_parameter
from types_definitions import QueryDefinition
from database import ReadOnlySession
```

### 3. تابع define()
```python
def define() -> QueryDefinition:
    """تعریف پارامترها و اسکیما"""
    parameters = [...]
    result_schema = schema(...)
    return {'parameters': parameters, 'schema': result_schema}
```

### 4. تابع execute()
```python
def execute(session: ReadOnlySession) -> List[Dict[str, Any]]:
    """اجرای آزمون"""
    # دریافت پارامترها
    # دریافت داده‌ها از دیتابیس
    # پردازش داده‌ها
    # بازگشت نتایج
    return data
```

## 💡 نکات و توصیه‌ها

### نوشتن توضیحات موثر

1. **مشخص باشید**: دقیقاً بگویید آزمون باید چه کاری انجام دهد
2. **جزئیات بدهید**: ستون‌ها، محاسبات، فیلترها را توضیح دهید
3. **مثال بزنید**: اگر ممکن است مثال‌های عینی بزنید
4. **ساختار مشخص کنید**: نحوه مرتب‌سازی و فرمت خروجی را بگویید

### انتخاب مدل مناسب

| نیاز | مدل پیشنهادی | دلیل |
|------|--------------|------|
| آزمون ساده | GPT-3.5-turbo | سرعت بالا، هزینه کم |
| آزمون پیچیده | GPT-4 | کیفیت بهتر |
| آزمون بسیار پیچیده | Claude-3-Opus | تحلیل عمیق‌تر |

### مدیریت هزینه

- از GPT-3.5 برای آزمون‌های ساده استفاده کنید
- ابتدا با مدل ارزان‌تر تست کنید
- توضیحات خود را مختصر و مفید بنویسید
- از cache کردن نتایج استفاده کنید (اگر همان آزمون را چند بار می‌خواهید)

## 🔧 عیب‌یابی

### خطای "Library not installed"

اگر AI کتابخانه‌ای استفاده کرد که نصب نیست:

```bash
pip install <library-name>
```

### کد تولیدی کار نمی‌کند

1. کد تولید شده را بررسی کنید
2. خطاهای syntax را اصلاح کنید
3. imports گم‌شده را اضافه کنید
4. با توضیحات دقیق‌تر دوباره تولید کنید

### خطای API

- اطمینان حاصل کنید کلید API صحیح است
- بررسی کنید اعتبار حساب شما کافی است
- اتصال اینترنت را چک کنید

## 📚 مثال‌های کاربردی

### مثال 1: تراکنش‌های مشکوک آخر هفته

**توضیحات:**
```
آزمون تشخیص تراکنش‌های انجام شده در روزهای آخر هفته (جمعه و شنبه).
- فیلتر کردن تراکنش‌ها بر اساس روز هفته
- نمایش تاریخ، مبلغ، کد حساب و توضیحات
- محاسبه مجموع مبلغ تراکنش‌های مشکوک
- مرتب‌سازی نزولی بر اساس مبلغ
```

**نام فایل:** `weekend_transactions_test`

### مثال 2: تحلیل نوسانات قیمت

**توضیحات:**
```
آزمون تحلیل نوسانات قیمت خرید برای هر کالا.
- گروه‌بندی تراکنش‌ها بر اساس کد کالا
- محاسبه میانگین، حداقل، حداکثر و انحراف معیار قیمت
- محاسبه ضریب تغییرات (CV)
- فقط کالاهایی با CV بیش از 0.2 را نمایش بده
- شامل تعداد تراکنش‌ها برای هر کالا
```

**نام فایل:** `price_volatility_analysis_test`

### مثال 3: الگوی معاملات فصلی

**توضیحات:**
```
آزمون تحلیل الگوی فصلی فروش.
- گروه‌بندی فروش‌ها بر اساس فصل (بهار، تابستان، پاییز، زمستان)
- محاسبه مجموع فروش هر فصل
- محاسبه درصد هر فصل از کل فروش
- نمایش میانگین فروش روزانه هر فصل
- مقایسه با میانگین کل سال
```

**نام فایل:** `seasonal_sales_pattern_test`

## 🔐 امنیت

- کلیدهای API هیچ‌گاه ذخیره نمی‌شوند
- ارتباطات از طریق HTTPS رمزگذاری می‌شوند
- فقط کاربران احراز هویت شده می‌توانند از آزمون‌ساز استفاده کنند
- کد تولیدی قبل از ذخیره بررسی می‌شود

## 📞 پشتیبانی

اگر مشکلی دارید:

1. مستندات را دوباره بخوانید
2. مثال‌های موجود را بررسی کنید
3. با تیم پشتیبانی تماس بگیرید

## 🔄 به‌روزرسانی‌های آینده

- [ ] پشتیبانی از مدل‌های محلی (Local LLMs)
- [ ] قالب‌های از پیش تعریف شده
- [ ] بهبود خودکار کد تولید شده
- [ ] تاریخچه آزمون‌های تولید شده
- [ ] امکان ویرایش و بازتولید

---

**نسخه:** 1.0  
**تاریخ:** 1403  
**سازنده:** سیستم آزمون‌های حسابرسی
